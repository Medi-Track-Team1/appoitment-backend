# ===============================
# SPRING BOOT APPLICATION CONFIG
# ===============================
server.port=8085
spring.application.name=meditrack-backend

# ===============================
# MONGODB ATLAS CONFIGURATION
# ===============================
# Recommended: SRV connection string (use ENV variable in prod)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://anushiniananth:Anu%401234@cluster0.rueiggi.mongodb.net/meditrack?retryWrites=true&w=majority}

# Automatically create indexes defined in entities
spring.data.mongodb.auto-index-creation=true

# ===============================
# SPRING MVC CONFIGURATION
# ===============================
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=non_null

# ===============================
# LOGGING CONFIGURATION
# ===============================
logging.level.org.springframework=INFO
logging.level.com.appointment=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# ===============================
# CORS CONFIGURATION (Frontend access)
# ===============================
spring.web.cors.allowed-origins=http://localhost:5174,http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# ===============================
# APPOINTMENT CUSTOM PROPERTIES
# ===============================
appointment.default-duration=30    # minutes
appointment.emergency-slots=5      # reserved emergency slots per day

# ===============================
# EMAIL CONFIGURATION
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:meditrackhealthinfo@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:uvjjnrsrhcrujbkb}   # ?? Move this to ENV variable in prod
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===============================
# EUREKA CONFIGURATION (disabled for local development)
# ===============================
eureka.client.enabled=false
eureka.instance.enabled=false
