# ===============================
# SPRING BOOT APPLICATION CONFIG
# ===============================
server.port=8085
spring.application.name=backend

# ===============================
# MONGODB ATLAS CONFIGURATION (USE ONLY ONE CONNECTION METHOD)
# ===============================
# Choose EITHER MongoDB Atlas (cloud) OR local MongoDB - not both

# Option 1: MongoDB Atlas (cloud) - FIXED DATABASE NAME
spring.data.mongodb.uri=mongodb+srv://anushiniananth:Anu%401234@cluster0.rueiggi.mongodb.net/meditrack?retryWrites=true&w=majority

# Option 2: Local MongoDB (comment out the above line if using this)
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=meditrack

# Common MongoDB settings
spring.data.mongodb.auto-index-creation=true

# ===============================
# SPRING MVC CONFIGURATION
# ===============================
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=non_null

# ===============================
# LOGGING CONFIGURATION
# ===============================
logging.level.org.springframework=INFO
logging.level.meditrack=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# ===============================
# CORS CONFIGURATION (for frontend access)
# ===============================
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
# spring.web.cors.allowed-headers=*

# ===============================
# ADDITIONAL APP-SPECIFIC PROPERTIES
# ===============================
appointment.default-duration=30 # minutes
appointment.emergency-slots=5 # reserved emergency slots per day

# ===============================
# EMAIL CONFIGURATION (if needed)
# ===============================
# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=meditrackhealthinfo@gmail.com
spring.mail.password=ipyhqhbrampvrdph
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# For debug
logging.level.org.springframework.mail=DEBUG
logging.level.org.springframework.boot.autoconfigure.mail=DEBUG
